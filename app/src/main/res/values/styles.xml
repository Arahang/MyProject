<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat">
        <!-- Primary theme color of the app (sets background color of app bar) -->
        <item name="colorPrimary">#FF9800</item>
        <!-- Primary dark theme color of the app (sets color of status bar) -->
        <item name="colorPrimaryDark">#FB8C00</item>
        <!-- Background color of buttons in the app -->
        <item name="colorButtonNormal">#FF9800</item>
    </style>


    <!-- for the sliding tab and sliding strip -->
    <style name="AppTheme2" parent="Theme.AppCompat.NoActionBar">
       <!-- <item name="windowActionBar">false</item> -->
    </style>


    <!-- for the circle Page Indicator  -->
    <style name="CircleIndicators" parent="@android:style/Theme.Light">
        <item name="vpiCirclePageIndicatorStyle">@style/CustomCirclePageIndicator</item>
    </style>

    <style name="CustomCirclePageIndicator">
        <item name="fillColor">#FFFFFF</item>
        <item name="strokeColor">#FF888888</item>
        <item name="strokeWidth">2dp</item>
        <item name="radius">5dp</item>
        <item name="centered">true</item>
        <item name="pageColor">#FF888888</item>
    </style>

</resources>
